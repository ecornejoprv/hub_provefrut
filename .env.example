# =================================================================
# PLANTILLA DE CONFIGURACIÓN - HUB PROVEFRUT
# =================================================================
# INSTRUCCIONES PARA DEVOPS/CONSULTORES:
# 1. Copiar este archivo a .env
# 2. Reemplazar los valores de ejemplo con credenciales reales.
# =================================================================

# --- SEGURIDAD DE DJANGO ---
# Generar una cadena aleatoria larga (50+ caracteres).
SECRET_KEY=cambiar_esto_por_una_clave_segura_y_unica

# En Producción SIEMPRE debe ser False.
DEBUG=False

# Lista de dominios/IPs que pueden servir la app (separados por coma).
ALLOWED_HOSTS=localhost,127.0.0.1,api.provefrut.com

# --- BASE DE DATOS (POSTGRESQL) ---
DB_NAME=hub_identidad_db
DB_USER=postgres
DB_PASSWORD=password_secreto
DB_HOST=localhost
DB_PORT=5432

# --- SEGURIDAD WEB (CORS & CSRF) ---
# Dominios desde donde se permite acceder a la API (Frontend React).
# Ejemplo: https://hub.provefrut.com,http://localhost
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173