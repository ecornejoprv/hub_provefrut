services:
  # --- BACKEND (Django API) ---
  backend:
    container_name: hub_backend_prod
    build: 
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env  
    networks:
      - hub_net

  # --- FRONTEND (React + Nginx) ---
  frontend:
    container_name: hub_frontend_prod
    build: 
      context: ./portal_web
      dockerfile: Dockerfile
      # Pasamos las variables para que React sepa a dónde apuntar los botones
      args:
        - VITE_URL_COMPRAS=${VITE_URL_COMPRAS}
        - VITE_URL_CHATBOT=${VITE_URL_CHATBOT}
    restart: always
    ports:
      - "9000:80" # Mapea el puerto 80 del contenedor al puerto 9000 del host
    depends_on:
      - backend
    networks:
      - hub_net

networks:
  hub_net:
    driver: bridge